// Legacy include path kept for callers that still expect Intersection.glsl.
// The default path tracing variant lives here; other modes include their own explicit entry files.
#include "IntersectionPathTracing.glsl"
