// Legacy include path kept for callers that still expect MissCommon.glsl.
// The default path tracing variant lives here; AO and direct lighting have explicit entry files.
#include "MissPathTracing.glsl"
