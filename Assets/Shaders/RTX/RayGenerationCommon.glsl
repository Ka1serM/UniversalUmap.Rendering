// Legacy include path kept for callers that still expect RayGenerationCommon.glsl.
// The default path tracing variant lives here; AO and direct lighting have explicit entry files.
#include "RayGenerationPathTracing.glsl"
