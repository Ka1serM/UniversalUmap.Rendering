// Legacy include path kept for callers that still expect ClosestHitCommon.glsl.
// The default path tracing variant lives here; AO and direct lighting have explicit entry files.
#include "ClosestHitPathTracing.glsl"
