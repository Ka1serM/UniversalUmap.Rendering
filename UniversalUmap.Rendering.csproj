<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>UniversalUmap.Rendering</RootNamespace>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <ProduceReferenceAssembly>false</ProduceReferenceAssembly>
  </PropertyGroup>

  <PropertyGroup>
    <DisableRtx Condition="'$(DisableRtx)' == ''">false</DisableRtx>
  </PropertyGroup>

  <PropertyGroup Condition="'$(DisableRtx)' == 'true'">
    <DefineConstants>$(DefineConstants);DISABLE_RTX</DefineConstants>
  </PropertyGroup>

  <PropertyGroup Condition="'$(RuntimeIdentifier)' != ''">
    <AppendRuntimeIdentifierToOutputPath>true</AppendRuntimeIdentifierToOutputPath>
  </PropertyGroup>

  <PropertyGroup Condition=" '$(Configuration)' == 'Release' ">
    <PlatformTarget>x64</PlatformTarget>
    <DebugType>none</DebugType>
  </PropertyGroup>

  <PropertyGroup>
    <SlangcPath Condition="'$(SlangcPath)' == '' and '$(VULKAN_SDK)' != '' and Exists('$(VULKAN_SDK)\Bin\slangc.exe')">$(VULKAN_SDK)\Bin\slangc.exe</SlangcPath>
    <SlangcPath Condition="'$(SlangcPath)' == '' and '$(VULKAN_SDK)' != '' and Exists('$(VULKAN_SDK)/bin/slangc')">$(VULKAN_SDK)/bin/slangc</SlangcPath>
    <SlangcPath Condition="'$(SlangcPath)' == '' and '$(VULKAN_SDK)' != '' and Exists('/var/home/$([System.Environment]::GetEnvironmentVariable(&quot;USER&quot;))/Programs/Vulkan/$([System.IO.Path]::GetFileName(&quot;$(VULKAN_SDK)&quot;))/x86_64/bin/slangc')">/var/home/$([System.Environment]::GetEnvironmentVariable(&quot;USER&quot;))/Programs/Vulkan/$([System.IO.Path]::GetFileName(&quot;$(VULKAN_SDK)&quot;))/x86_64/bin/slangc</SlangcPath>
    <SlangcPath Condition="'$(SlangcPath)' == '' and '$(VULKAN_SDK)' != '' and Exists('/home/$([System.Environment]::GetEnvironmentVariable(&quot;USER&quot;))/Programs/Vulkan/$([System.IO.Path]::GetFileName(&quot;$(VULKAN_SDK)&quot;))/x86_64/bin/slangc')">/home/$([System.Environment]::GetEnvironmentVariable(&quot;USER&quot;))/Programs/Vulkan/$([System.IO.Path]::GetFileName(&quot;$(VULKAN_SDK)&quot;))/x86_64/bin/slangc</SlangcPath>
    <SlangcPath Condition="'$(SlangcPath)' == ''">slangc</SlangcPath>
    <SlangBinDir>$([System.IO.Path]::GetDirectoryName(&quot;$(SlangcPath)&quot;))</SlangBinDir>
    <SlangLibDir>$([System.IO.Path]::GetFullPath($([System.IO.Path]::Combine(&quot;$(SlangBinDir)&quot;, &quot;..&quot;, &quot;lib&quot;))))</SlangLibDir>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Avalonia" Version="11.3.12" />
    <PackageReference Include="Avalonia.Skia" Version="11.3.12" />
    <PackageReference Include="CommunityToolkit.Mvvm" Version="8.4.0" />
    <PackageReference Include="FluentIcons.Avalonia.Fluent" Version="2.0.318" />
    <PackageReference Include="Serilog" Version="4.3.1" />
    <PackageReference Include="Silk.NET.Vulkan" Version="2.23.0" />
    <PackageReference Include="Silk.NET.Vulkan.Extensions.EXT" Version="2.23.0" />
    <PackageReference Include="Silk.NET.Vulkan.Extensions.KHR" Version="2.23.0" />
    <PackageReference Include="StbImageSharp" Version="2.30.15" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\CUE4Parse\CUE4Parse\CUE4Parse.csproj" />
    <ProjectReference Include="..\CUE4Parse\CUE4Parse-Conversion\CUE4Parse-Conversion.csproj" />
  </ItemGroup>

  <ItemGroup>
    <ShaderInclude Include="$(ProjectDir)Assets\Shaders\**\*.slang" />
    <ShaderInclude Include="$(ProjectDir)Assets\Shaders\**\*.h" />

    <!-- Minimal entry-point list; stage is declared in shader attributes -->
    <ShaderSource Include="$(ProjectDir)Assets/Shaders/AO/*Raygen.slang" />
    <ShaderSource Include="$(ProjectDir)Assets/Shaders/AO/*Closesthit.slang" />
    <ShaderSource Include="$(ProjectDir)Assets/Shaders/AO/*Miss.slang" />
    <ShaderSource Include="$(ProjectDir)Assets/Shaders/AO/*Compute.slang">
      <Arguments>-DUSE_COMPUTE</Arguments>
    </ShaderSource>
    <ShaderSource Include="$(ProjectDir)Assets/Shaders/DirectLighting/*Raygen.slang" />
    <ShaderSource Include="$(ProjectDir)Assets/Shaders/DirectLighting/*Closesthit.slang" />
    <ShaderSource Include="$(ProjectDir)Assets/Shaders/DirectLighting/*Miss.slang" />
    <ShaderSource Include="$(ProjectDir)Assets/Shaders/DirectLighting/*Compute.slang">
      <Arguments>-DUSE_COMPUTE</Arguments>
    </ShaderSource>
    <ShaderSource Include="$(ProjectDir)Assets/Shaders/PathTracing/*Raygen.slang" />
    <ShaderSource Include="$(ProjectDir)Assets/Shaders/PathTracing/*Closesthit.slang" />
    <ShaderSource Include="$(ProjectDir)Assets/Shaders/PathTracing/*Miss.slang" />
    <ShaderSource Include="$(ProjectDir)Assets/Shaders/PathTracing/*Compute.slang">
      <Arguments>-DUSE_COMPUTE</Arguments>
    </ShaderSource>
    <ShaderSource Include="$(ProjectDir)Assets/Shaders/Lights/ShadowMiss.slang" />
    <ShaderSource Include="$(ProjectDir)Assets/Shaders/Compositing/Compositor.slang" />
    <ShaderSource Include="$(ProjectDir)Assets/Shaders/Widgets/*VS.slang" />
    <ShaderSource Include="$(ProjectDir)Assets/Shaders/Widgets/*FS.slang" />

  </ItemGroup>

  <Target Name="CompileShaders"
          BeforeTargets="PrepareResources"
          Inputs="@(ShaderSource);@(ShaderInclude)"
          Outputs="%(ShaderSource.RootDir)%(ShaderSource.Directory)%(ShaderSource.Filename).spv">

    <Exec Command='env LD_LIBRARY_PATH="$(SlangLibDir):$$LD_LIBRARY_PATH" PATH="$(SlangBinDir):$$PATH" "$(SlangcPath)" -version'
          IgnoreExitCode="true">
      <Output TaskParameter="ExitCode" PropertyName="SlangcVersionExitCode" />
    </Exec>
    <Error Condition="'$(SlangcVersionExitCode)' != '0'"
           Text="Could not execute slangc. Set SlangcPath explicitly or install it in VULKAN_SDK/bin." />

    <Message Text='Compiling shaders via "$(SlangcPath)"...' Importance="High" />

    <Exec Command='env LD_LIBRARY_PATH="$(SlangLibDir):$$LD_LIBRARY_PATH" PATH="$(SlangBinDir):$$PATH" "$(SlangcPath)" -target spirv -profile glsl_460 -entry main %(ShaderSource.Arguments) "%(ShaderSource.Identity)" -o "%(ShaderSource.RootDir)%(ShaderSource.Directory)%(ShaderSource.Filename).spv"'
          LogStandardErrorAsError="true" />

  </Target>

  <Target Name="IncludeCompiledShadersAsResources"
          DependsOnTargets="CompileShaders"
          BeforeTargets="CoreCompile">
    <ItemGroup>
      <EmbeddedResource Include="Assets\Shaders\**\*.spv"
                        Exclude="@(EmbeddedResource)">
        <LogicalName>$([System.String]::Copy('UniversalUmap.Rendering.Assets.%(RecursiveDir)%(Filename)%(Extension)').Replace('\','.').Replace('/','.'))</LogicalName>
      </EmbeddedResource>
    </ItemGroup>
  </Target>

  <Target Name="GenerateSharedStructsHeader"
          BeforeTargets="CompileShaders"
          Inputs="$(ProjectDir)Assets\Shaders\SharedStructs.cs;$(MSBuildProjectDirectory)\..\Tools\SharedStructsHeaderGen\Program.cs;$(MSBuildProjectDirectory)\..\Tools\SharedStructsHeaderGen\SharedStructsHeaderGen.csproj"
          Outputs="$(ProjectDir)Assets\Shaders\SharedStructs.slang">
    <Message Text="Generating SharedStructs.slang from Assets\Shaders\SharedStructs.cs..." Importance="High" />
    <Exec Command='dotnet run --project "$(MSBuildProjectDirectory)\..\Tools\SharedStructsHeaderGen\SharedStructsHeaderGen.csproj" -- "$(ProjectDir)Assets\Shaders\SharedStructs.cs" "$(ProjectDir)Assets\Shaders\SharedStructs.slang"'
          LogStandardErrorAsError="true" />
  </Target>

  <Target Name="GenerateDefaultEnvironmentMaps"
          BeforeTargets="Build"
          Inputs="$(ProjectDir)Assets\Source\kiara_1_dawn_4k.hdr;$(MSBuildProjectDirectory)\..\Tools\EnvMapPrecompute\Program.cs;$(MSBuildProjectDirectory)\..\Tools\EnvMapPrecompute\EnvMapPrecompute.csproj"
          Outputs="$(ProjectDir)Assets\Textures\kiara_1_dawn_4k_env.bin;$(ProjectDir)Assets\Textures\kiara_1_dawn_4k_cdf.bin;$(ProjectDir)Assets\Textures\kiara_1_dawn_4k_irradiance.bin;$(ProjectDir)Assets\Textures\kiara_1_dawn_4k_radiance.bin">
    <Message Text="Precompiling default HDR environment maps..." Importance="High" />
    <Exec Command='dotnet run --project "$(MSBuildProjectDirectory)\..\Tools\EnvMapPrecompute\EnvMapPrecompute.csproj" -- "$(ProjectDir)Assets\Source\kiara_1_dawn_4k.hdr" "$(ProjectDir)Assets\Textures\kiara_1_dawn_4k"'
          LogStandardErrorAsError="true" />
  </Target>

  <Target Name="CleanCompiledShaders" BeforeTargets="Clean">
    <ItemGroup>
      <CompiledSpv Include="$(ProjectDir)Assets\Shaders\**\*.spv" />
    </ItemGroup>
    <Message Text="Removing compiled shader binaries (*.spv)..." Importance="High" />
    <Delete Files="@(CompiledSpv)" />
  </Target>

  <ItemGroup>
    <EmbeddedResource Include="Assets\**"
                      Exclude="Assets\Source\**;@(EmbeddedResource)">
      <LogicalName>$([System.String]::Copy('UniversalUmap.Rendering.Assets.%(RecursiveDir)%(Filename)%(Extension)').Replace('\','.').Replace('/','.'))</LogicalName>
    </EmbeddedResource>
  </ItemGroup>
</Project>
